{
  "name": "openhub-cloudflare-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate": "wrangler d1 migrations apply openhub --remote",
    "deploy:production": "npm run d1:migrate && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
